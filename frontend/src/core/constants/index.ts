export const APP_NAME = 'Catálogo de Carros';

export const ROUTES = {
  HOME: '/',
  NOT_FOUND: '*',
} as const;

export const API_ENDPOINTS = {
  EXTERNAL: '/external',
  INTERNAL: '/internal',
} as const;
